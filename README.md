# Mini app

# Requirements

    - Node.js
    - Ruby
      - bundle (`gem install bundler`)

# Getting started

Run a bundle install and setup the database:

    $ bundle install
    $ npm install

Run the app locally:

    $ npm run serve

# Running tests

    $ npm run test
